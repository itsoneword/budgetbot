# Core bot dependencies
# [job-queue] pulls APScheduler for the recurring-transactions scheduler (T-026);
# without the extra application.job_queue is None.
python-telegram-bot[job-queue]==22
python-dateutil==2.9.0

# Data processing & visualization
pandas==2.2.3
numpy==2.0.2
matplotlib==3.9.4
seaborn==0.13.2

# External APIs
yfinance==0.2.55

# Database (Phase 1 - PostgreSQL migration)
asyncpg==0.29.0

# Schema migrations (T-005) — alembic runs over asyncpg via SQLAlchemy async engine
alembic==1.13.2
SQLAlchemy==2.0.35

# Configuration & utilities (Phase 2+)
pydantic-settings>=2.5.2
aiofiles==23.2.1

# Logging (Phase 2+)
structlog==24.1.0
# AI Q&A /ask (T-018) — uses Claude Code subscription OAuth via mounted CLI
claude-agent-sdk>=0.1.80,<0.2
mcp>=1.20

# Voice transcription (T-019) — local CPU whisper, weights mounted from host HF cache
faster-whisper>=1.1,<2
